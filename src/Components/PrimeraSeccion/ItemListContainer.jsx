import React from 'react';

export const ItemListContainer = ({greeting}) => {
    return (
        <section className='container container-fluid mt-4'>
            {greeting}

        </section>
    );
}


